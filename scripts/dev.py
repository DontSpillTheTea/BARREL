def main():
    print("Starting development servers...")
    print("Currently, this is a placeholder.")
    print("Eventually, this script will concurrently run:")
    print("1. FastAPI backend (e.g., uvicorn apps.api.app.main:app --reload)")
    print("2. Vite frontend (e.g., npm run dev --prefix apps/web)")
    print("\nPlease ensure dependencies are installed via `make setup` once available.")

if __name__ == "__main__":
    main()
