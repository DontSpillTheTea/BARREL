# Samples

This directory holds sample alcohol beverage labels for testing the OCR and extraction pipeline.
Ensure no PII or confidential unreleased labels are committed here.

`scripts/build_sample_batches.py` is used to build zip batches for testing.
